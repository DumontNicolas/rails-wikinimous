<h1>Articles</h1>
<ul>
  <% Article.all.each do |article|%>
    <li><%= link_to article.title, article_path(article), style: "text-decoration: none; font-size:24px;" %></li>
    <em><%= article.content %></em>
  <% end %>
</ul>
